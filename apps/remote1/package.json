{
  "name": "remote1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "kill-port 3001 && webpack serve --mode development --port 3001",
    "build": "rimraf dist && webpack --mode production",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "rimraf ../../deploy/mfe/remote1 && copyfiles -f dist/* ../../deploy/mfe/remote1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "webpack-config": "workspace:*",
    "components": "workspace:*"
  }
}
