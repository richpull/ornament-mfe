import React, { useContext } from 'react';
// import { Context } from 'host/context';

export const Demo = () => {
  // const value = useContext(Context);

  return (
    <div>
      <input type="checkbox" />
      <div>I am demo 12</div>
      <div>{'value'}</div>
    </div>
  );
};

export default Demo;
